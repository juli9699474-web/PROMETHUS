[project]
name = "prometheus-ai"
version = "0.1.0"
description = "PROMETHEUS AI subsystem"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8.0",
  "torch>=2.3.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "ruff>=0.6.0",
  "mypy>=1.11.0",
]
