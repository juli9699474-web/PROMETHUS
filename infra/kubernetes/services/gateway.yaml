apiVersion: v1
kind: Service
metadata:
  name: prometheus-gateway
  namespace: prometheus-agi
spec:
  selector:
    app: prometheus-gateway
  ports:
    - name: http
      port: 3001
      targetPort: 3001
