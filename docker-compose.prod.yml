version: "3.9"
services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: always
  redis:
    image: redis:7-alpine
    restart: always
  gateway:
    build: ./gateway
    restart: always
    ports: ["3001:3001"]
  web:
    build: ./web
    restart: always
    ports: ["3000:3000"]
